import type { Metadata } from "next";

export const metadata: Metadata = { title: "Edit Sampling Route" };

export default function EditSamplingRouteLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}
