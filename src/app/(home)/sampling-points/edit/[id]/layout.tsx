import type { Metadata } from "next";

export const metadata: Metadata = { title: "Edit Sampling Point" };

export default function EditSamplingPointLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}
